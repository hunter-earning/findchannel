const config = {
    // Replace with your YouTube Data API key
    YOUTUBE_API_KEY: 'YOUR_API_KEY_HERE',
    
    // API endpoints
    YOUTUBE_API_BASE_URL: 'https://www.googleapis.com/youtube/v3',
    
    // Search parameters
    MAX_RESULTS: 50,
    
    // Activity threshold (in days) to determine if a channel is active
    ACTIVITY_THRESHOLD_DAYS: 30,
    
    // Video duration thresholds (in minutes)
    SHORT_VIDEO_MAX_DURATION: 1,
    LONG_VIDEO_MIN_DURATION: 5
}; 